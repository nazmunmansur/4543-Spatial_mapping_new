# ❓ Mini Quiz: 03 - Scalar Types and Control Flow

---

### 1. Which type is returned by this expression?

```python
type(True)
```
The expression shows bool
---

### 2. What will the following print?

```python
x = 7
print(x >= 7 and x < 10)
```

Answer: It will print True

---

### 3. What does `not` do in a boolean expression?

---It returns the opposite Boolean value. 

### 4. Fill in the blank to check for inequality:

```python
if score ___ 100:
    print("Perfect score!")
```
# Answer: 
if score == 100:
    print("Perfect score!")
---

### 5. What keyword starts a conditional chain after an `if` block?
#Answer:
else if” or “elif” condition to check the other statements sequentially.